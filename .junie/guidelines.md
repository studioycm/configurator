# Project instructions

Read and follow [../AGENTS.md](../AGENTS.md) in the project root before working on this application. It contains the current Laravel Boost guidance and skill instructions.

Use the versions installed in `composer.lock` and `package-lock.json`. The application uses Laravel 13, Livewire 4, and Filament 5; Livewire Volt is not installed.
